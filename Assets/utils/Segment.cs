using System.Collections;
using System.Collections.Generic;
using System.Linq;
using UnityEngine;

public class Segment : MonoBehaviour
{
   public Transform start, end;

   public void Reset()
   {
//      var list = GetComponentsInChildren<MoverActivator>().ToList();
//      foreach (var m in list)
//         m.StartGameplayPhase();
//
//      var list2 = GetComponentsInChildren<Collider>().ToList();
//      foreach (var c in list2)
//         c.gameObject.tag = "Enemy";
   }
}
